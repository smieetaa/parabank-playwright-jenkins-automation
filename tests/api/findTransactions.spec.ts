import { test, expect } from '@playwright/test';

test('Find transaction by amount', async ({ request }) => {
//   const response = await request.get('https://parabank.parasoft.com/parabank/services/bank/findTransactions?accountId=12345&amount=100');
//   expect(response.ok()).toBeTruthy();

//   const json = await response.json();
//   expect(Array.isArray(json)).toBe(true);
});
